{
  "name": "luz-performance-mvp",
  "version": "1.0.0",
  "description": "Orquestrador de scripts para o MVP Luz Performance",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:full": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "start": "start-all.bat"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
